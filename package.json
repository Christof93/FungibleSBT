{
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.2",
    "bigint-buffer": "^1.1.5"
  },
  "devDependencies": {
    "ganache": "^7.9.0",
    "ganache-cli": "^6.12.2",
    "solidity-coverage": "^0.7.22",
    "truffle": "^5.5.19"
  },
  "scripts": {
    "coverage": "truffle run coverage --network development",
    "compile": "truffle compile",
    "migrate": "truffle migrate --network development",
    "test": "GANACHE=$(ganache --detach) && truffle test; ganache instances stop $GANACHE"
  }
}
